# Creative Links Club — Mentor Portal

React + Vite + JavaScript/JSX + CSS + LocalStorage.

## Run

```bash
npm install
npm run dev
```

The application contains only the Mentors, Deployment and Processes pages, with leave, studio and venue management inside the relevant forms.
